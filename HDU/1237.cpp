#include<bits/stdc++.h>
using namespace std;

int main(){
    char ch;
    string s;
    while(cin>>ch&&ch!='0'){
        if(ch==' '||ch=='\n') continue;
        if(isdigit(ch)){
            s+=ch;
        }

        
    }
    return 0;
}